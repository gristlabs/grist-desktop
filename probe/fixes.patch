diff --git a/app/client/components/BaseView.ts b/app/client/components/BaseView.ts
index 7a6e1e2c5..9a43a709c 100644
--- a/app/client/components/BaseView.ts
+++ b/app/client/components/BaseView.ts
@@ -9,6 +9,7 @@ import { viewCommands } from "app/client/components/RegionFocusSwitcher";
 import { SelectionSummary } from "app/client/components/SelectionSummary";
 import { KoArray } from "app/client/lib/koArray";
 import * as tableUtil from "app/client/lib/tableUtil";
+import { testPendingViewLoads } from "app/client/lib/testPendingOps";
 import BaseRowModel from "app/client/models/BaseRowModel";
 import { ClientColumnGetters } from "app/client/models/ClientColumnGetters";
 import { DataRowModel } from "app/client/models/DataRowModel";
@@ -262,6 +263,16 @@ export default class BaseView extends DisposableWithEvents {
     // --------------------------------------------------
     // Observables local to this view
     this._isLoading = ko.observable(true);
+
+    // Counted on the change, not per query: a superseded query never calls back, and the flag
+    // stays set until the latest one does.
+    testPendingViewLoads.start();
+    this.autoDispose(this._isLoading.subscribe((isLoading: boolean) => {
+      if (isLoading) { testPendingViewLoads.start(); } else { testPendingViewLoads.end(); }
+    }));
+    this.onDispose(() => {
+      if (this._isLoading.peek()) { testPendingViewLoads.end(); }
+    });
     this._pendingCursorPos = {
       cursorPos: this.viewSection.lastCursorPos,
       showFirstRowIfRowMissing: true,
diff --git a/app/client/lib/GristWindow.ts b/app/client/lib/GristWindow.ts
index f3d73deb2..67cee4a2f 100644
--- a/app/client/lib/GristWindow.ts
+++ b/app/client/lib/GristWindow.ts
@@ -27,6 +27,8 @@ declare global {
       testNumPendingApiRequests?: () => number;
       testNumPendingChecks?: () => number;
       testNumPendingPastes?: () => number;
+      testNumPendingMenuActions?: () => number;
+      testNumPendingViewLoads?: () => number;
     };
     cmd?: { [name: string]: () => void };
     isRunningUnderElectron?: boolean;
diff --git a/app/client/lib/testPendingOps.ts b/app/client/lib/testPendingOps.ts
index 57d638cf6..046ba52b7 100644
--- a/app/client/lib/testPendingOps.ts
+++ b/app/client/lib/testPendingOps.ts
@@ -40,3 +40,15 @@ export const testPendingChecks = new TestPendingOps();
  * in-flight request counts go quiet between the two and look like success.
  */
 export const testPendingPastes = new TestPendingOps();
+
+/**
+ * Menu items that defer their action to the next tick, so the menu can close first. Nothing is in
+ * flight in that gap, so a test that reloads there loses the action.
+ */
+export const testPendingMenuActions = new TestPendingOps();
+
+/**
+ * Views waiting for data. A cursor move makes the widgets linked to it re-query, and nothing in
+ * the page waits for that.
+ */
+export const testPendingViewLoads = new TestPendingOps();
diff --git a/app/client/ui/App.ts b/app/client/ui/App.ts
index a38c607bd..60c782ca8 100644
--- a/app/client/ui/App.ts
+++ b/app/client/ui/App.ts
@@ -12,7 +12,8 @@ import { onClickOutside } from "app/client/lib/domUtils";
 import { FocusLayer } from "app/client/lib/FocusLayer";
 import * as koUtil from "app/client/lib/koUtil";
 import { makeT } from "app/client/lib/localization";
-import { testPendingChecks, testPendingPastes } from "app/client/lib/testPendingOps";
+import { testPendingChecks, testPendingMenuActions, testPendingPastes, testPendingViewLoads }
+  from "app/client/lib/testPendingOps";
 import { reportError, TopAppModel, TopAppModelImpl } from "app/client/models/AppModel";
 import { DocPageModel } from "app/client/models/DocPageModel";
 import { setUpErrorHandling } from "app/client/models/errors";
@@ -276,6 +277,14 @@ export class AppImpl extends DisposableWithEvents implements App {
     return testPendingPastes.count;
   }
 
+  public testNumPendingMenuActions(): number {
+    return testPendingMenuActions.count;
+  }
+
+  public testNumPendingViewLoads(): number {
+    return testPendingViewLoads.count;
+  }
+
   private _reloadPane() {
     console.log("reloadPane");
     this.topAppModel.reload();
diff --git a/app/client/ui2018/menus.ts b/app/client/ui2018/menus.ts
index 0b070be98..195f9b05e 100644
--- a/app/client/ui2018/menus.ts
+++ b/app/client/ui2018/menus.ts
@@ -1,6 +1,7 @@
 import { MenuCommand } from "app/client/components/commandList";
 import { FocusLayer } from "app/client/lib/FocusLayer";
 import { makeT } from "app/client/lib/localization";
+import { testPendingMenuActions } from "app/client/lib/testPendingOps";
 import { NeedUpgradeError, reportError } from "app/client/models/errors";
 import { textButton } from "app/client/ui2018/buttons";
 import { cssCheckboxSquare, cssLabel, cssLabelText } from "app/client/ui2018/checkbox";
@@ -591,7 +592,19 @@ export function menuItemTrimmed(
  * can be removed.
  */
 export const menuItemAsync: typeof weasel.menuItem = function(action, ...args) {
-  return menuItem(() => setTimeout(action, 0), ...args);
+  // The timer calls the action with no arguments, as a bare setTimeout(action, 0) did.
+  const run = action as unknown as () => unknown;
+  return menuItem(() => {
+    // Counted before yielding, so the action is never observed as not pending.
+    testPendingMenuActions.start();
+    setTimeout(async () => {
+      try {
+        await run();
+      } finally {
+        testPendingMenuActions.end();
+      }
+    }, 0);
+  }, ...args);
 };
 
 export function menuItemCmd(
diff --git a/test/nbrowser/CopyPasteLinked.ts b/test/nbrowser/CopyPasteLinked.ts
index a9d637fd8..97b40a55f 100644
--- a/test/nbrowser/CopyPasteLinked.ts
+++ b/test/nbrowser/CopyPasteLinked.ts
@@ -28,6 +28,10 @@ describe("CopyPasteLinked", function() {
     assert.equal(await cell.getText(), "John Malik");
 
     await clipboard.lockAndPerform(async (cb) => {
+      // The copy handler sits on the grid's hidden textarea, so a copy sent while anything else
+      // holds the focus is dropped, leaving the previous suite's clipboard contents to be pasted
+      // below.
+      await gu.waitAppFocus();
       // Copy the cell's value to the clipboard.
       await cb.copy();
 
diff --git a/test/nbrowser/Pages.ts b/test/nbrowser/Pages.ts
index 943d1918a..ef9b2f7af 100644
--- a/test/nbrowser/Pages.ts
+++ b/test/nbrowser/Pages.ts
@@ -4,6 +4,7 @@ import { Session } from "test/nbrowser/gristUtils";
 import * as gu from "test/nbrowser/gristUtils";
 import { server, setupTestSuite } from "test/nbrowser/testUtils";
 
+import isEqual from "lodash/isEqual";
 import values from "lodash/values";
 import { assert, driver, Key } from "mocha-webdriver";
 
@@ -57,8 +58,7 @@ describe("Pages", function() {
     const revertAcl = await gu.beginAclTran(api, doc.id);
     // Update ACL, hide Overview table from all users.
     await hideTable("Overview");
-    // We will be reloaded, but it's not easy to wait for it, so do the refresh manually.
-    await gu.reloadDoc();
+    await waitForPages(["Interactions", "Documents", "People", "User & Leads"]);
     assert.deepEqual(await gu.getPageTree(), [
       {
         label: "Interactions", children: [
@@ -74,7 +74,7 @@ describe("Pages", function() {
 
     // Now hide User_Leads
     await hideTable("User_Leads");
-    await gu.reloadDoc();
+    await waitForPages(["Interactions", "Documents", "People"]);
     assert.deepEqual(await gu.getPageTree(), [
       {
         label: "Interactions", children: [
@@ -88,7 +88,7 @@ describe("Pages", function() {
 
     // Now hide People, and test that whole node is hidden.
     await hideTable("People");
-    await gu.reloadDoc();
+    await waitForPages(["Interactions", "Documents"]);
     assert.deepEqual(await gu.getPageTree(), [
       {
         label: "Interactions", children: [
@@ -99,7 +99,7 @@ describe("Pages", function() {
 
     // Now hide Documents, this is a leaf, so it should be hidden from the start
     await hideTable("Documents");
-    await gu.reloadDoc();
+    await waitForPages(["Interactions"]);
     assert.deepEqual(await gu.getPageTree(), [
       {
         label: "Interactions",
@@ -108,13 +108,12 @@ describe("Pages", function() {
 
     // Now hide Interactions, we should have a blank treeview
     await hideTable("Interactions");
-    // We can wait for doc to load, because it waits for section.
-    await driver.findWait(".test-treeview-container", 1000);
+    await waitForPages([]);
     assert.deepEqual(await gu.getPageTree(), []);
 
     // Rollback
     await revertAcl();
-    await gu.reloadDoc();
+    await waitForPages(["Interactions", "Documents", "People", "User & Leads", "Overview"]);
     assert.deepEqual(await gu.getPageTree(), [
       {
         label: "Interactions", children: [
@@ -390,6 +389,7 @@ describe("Pages", function() {
       assert.deepEqual(await gu.getPageNames(), ["Interactions", "", "People", "User & Leads", "Overview"]));
     await gu.openPageMenu("Interactions");
     await gu.findOpenMenuItem(".test-docpage-collapse-by-default", "Set default: Collapse").click();
+    await gu.waitForMenuAction();
     await gu.waitForServer();
     assert.deepEqual(await gu.getPageNames(), ["Interactions", "", "People", "User & Leads", "Overview"]);
     await driver.navigate().refresh();
@@ -409,6 +409,7 @@ describe("Pages", function() {
       assert.deepEqual(await gu.getPageNames(), ["Interactions", "Documents", "People", "User & Leads", "Overview"]));
     await gu.openPageMenu("Interactions");
     await gu.findOpenMenuItem(".test-docpage-expand-by-default", "Set default: Expand").click();
+    await gu.waitForMenuAction();
     await gu.waitForServer();
     assert.deepEqual(await gu.getPageNames(), ["Interactions", "Documents", "People", "User & Leads", "Overview"]);
     await driver.navigate().refresh();
@@ -696,6 +697,25 @@ describe("Pages", function() {
     assert.deepEqual(await gu.getSectionTitles(), ["TABLE C", "TABLE D", "TABLE1"]);
   });
 
+  /**
+   * Waits for the page tree to settle on the given names, which is what hiding a table leads to.
+   * Hiding one makes the app reload the document itself, and the tree is emptied before it is
+   * rebuilt, so waiting for a page to be gone is not enough.
+   */
+  async function waitForPages(expected: string[]) {
+    await driver.wait(async () => {
+      try {
+        return await driver.find(".test-treeview-container").isPresent() &&
+          isEqual(await gu.getPageNames(), expected);
+      } catch (e) {
+        // The tree is rebuilt under the read, which makes its elements stale; look again. Other
+        // failures are the driver's, and end the wait.
+        if (e.name !== "StaleElementReferenceError") { throw e; }
+        return false;
+      }
+    }, 10000, `pages did not settle as ${expected.join(", ")}`);
+  }
+
   async function hideTable(tableId: string) {
     await api.applyUserActions(doc.id, [
       ["AddRecord", "_grist_ACLResources", -1, { tableId, colIds: "*" }],
diff --git a/test/nbrowser/ReferenceList.ts b/test/nbrowser/ReferenceList.ts
index 1de092717..ffe5f6a77 100644
--- a/test/nbrowser/ReferenceList.ts
+++ b/test/nbrowser/ReferenceList.ts
@@ -548,7 +548,14 @@ describe("ReferenceList", function() {
 
   describe("autocomplete", function() {
     const getACOptions = stackWrapFunc(async (limit?: number) => {
+      // Both waits are needed. An item says a dropdown is up; the search text says whose results
+      // it holds, since a list left over from the previous keystroke looks identical. The search
+      // text cannot separate two searches for the same text, which is why the first wait stays.
+      // Call this while the typed text is still in the box: selecting an item with the arrow keys
+      // or the mouse writes that item back into it without searching again.
       await driver.findWait(".test-ref-editor-item", 2000);
+      const text = await driver.find(".cell_editor .test-tokenfield .test-tokenfield-input").value();
+      await gu.autocomplete.wait(text);
       return (await driver.findAll(".test-ref-editor-item", el => el.getText())).slice(0, limit);
     });
 
@@ -777,6 +784,9 @@ describe("ReferenceList", function() {
       await driver.findContentWait(".test-treeview-itemHeader", /Colors/, 2000).click();
       await gu.waitForDocToLoad();
       await gu.sendKeys(Key.chord(await gu.modKey(), Key.ARROW_DOWN));
+      // Jumping to the last row scrolls it into the rendered window, and the rows read below are
+      // only there once it has. Waiting for the values avoids naming a row number.
+      await driver.findContentWait(".active_section .field_clip", /^almost pink$/, 2000);
       assert.deepEqual(
         await gu.getVisibleGridCells("Color Name", [146, 147]),
         ["pinkish", "almost pink"],
diff --git a/test/nbrowser/RowMenu.ts b/test/nbrowser/RowMenu.ts
index 6a8770adb..26fea48a1 100644
--- a/test/nbrowser/RowMenu.ts
+++ b/test/nbrowser/RowMenu.ts
@@ -25,6 +25,7 @@ describe("RowMenu", function() {
     await firstRow.find(".test-row-menu-trigger").click();
     assert.isTrue(await gu.findOpenMenu(1000).isDisplayed());
     // close the menu
+    await gu.waitForMenuFocus();
     await driver.sendKeys(Key.ESCAPE);
     // make sure the menu is closed
     await driver.wait(async () => (await driver.findAll(".grist-floating-menu")).length === 0, 2000);
@@ -67,6 +68,7 @@ describe("RowMenu", function() {
     assert.isTrue(await gu.findOpenMenu(1000).isDisplayed());
 
     // send ESC to close the menu
+    await gu.waitForMenuFocus();
     await driver.sendKeys(Key.ESCAPE);
 
     // check that the context menu is gone
diff --git a/test/nbrowser/ToggleColumns.ts b/test/nbrowser/ToggleColumns.ts
index 37fa0899c..8d0e99ae3 100644
--- a/test/nbrowser/ToggleColumns.ts
+++ b/test/nbrowser/ToggleColumns.ts
@@ -53,6 +53,7 @@ describe("ToggleColumns", function() {
   async function verifyToggleBehavior() {
     // Selecting a cell in Src should show only linked values in Items.
     await gu.getCell({ section: "Src", col: "A", rowNum: 1 }).click();
+    await gu.waitForViewLoads();
     assert.deepEqual(await gu.getVisibleGridCells({ section: "Items", cols: ["A", "Chk2"], rowNums: [1, 2, 3] }), [
       "Src[1]", "false",
       "Src[1]", "false",
@@ -73,6 +74,7 @@ describe("ToggleColumns", function() {
 
     // Try another row of table Src. It should have its own Items (initially none).
     await gu.getCell({ section: "Src", col: "A", rowNum: 2 }).click();
+    await gu.waitForViewLoads();
     assert.deepEqual(await gu.getVisibleGridCells({ section: "Items", cols: ["A", "Chk2"], rowNums: [1] }),
       ["", ""]);
 
diff --git a/test/nbrowser/gristUtils.ts b/test/nbrowser/gristUtils.ts
index c6fdbe3b0..182068156 100644
--- a/test/nbrowser/gristUtils.ts
+++ b/test/nbrowser/gristUtils.ts
@@ -712,6 +712,14 @@ namespace gristUtils {
     if (![Key.ENTER, Key.TAB].includes(lastKey!) && validate) {
       keys.push(Key.ENTER);
     }
+    // An editor on its way out still counts as open, so asking for one does nothing and the wait
+    // below never sees the focus move. Observed in failing runs: an editor present with the focus
+    // back on the grid, gone a hundred or so milliseconds later, with nothing to replace it. Only
+    // wait out an editor that has lost the focus: callers do type into one that still holds it.
+    await driver.wait(async () => await driver.executeScript<boolean>(() => {
+      const editors = Array.from(document.querySelectorAll(".cell_editor"));
+      return editors.length === 0 || editors.some(e => e.contains(document.activeElement));
+    }).catch(() => false), 2000, "an editor was closing and did not go");
     await driver.executeScript((...args: any[]) => {
       (window as any).gristApp.allCommands.input.run(...args);
     }, ...(clear ? [""] : []));
@@ -4342,6 +4350,35 @@ namespace gristUtils {
     await waitForPendingOps("testNumPendingPastes", "paste to complete", optTimeout);
   }
 
+  /**
+   * Waits for an open menu to hold the focus, which is when it starts handling keys. A menu takes
+   * focus on the tick after it is shown, so a key sent as soon as one appears can go elsewhere,
+   * and Escape in particular is then lost and the menu stays open.
+   */
+  export async function waitForMenuFocus(optTimeout: number = 1000) {
+    await driver.wait(async () => await driver.executeScript<boolean>(() => {
+      const active = document.activeElement;
+      return !!active && Array.from(document.querySelectorAll(".grist-floating-menu"))
+        .some(menu => menu === active || menu.contains(active));
+    }).catch(() => false), optTimeout, "the open menu did not take the focus");
+  }
+
+  /**
+   * Waits for every view to have the data it asked for. A cursor move makes the widgets linked to
+   * it re-query, and nothing in the page waits for that.
+   */
+  export async function waitForViewLoads(optTimeout: number = 5000) {
+    await waitForPendingOps("testNumPendingViewLoads", "views to load their data", optTimeout);
+  }
+
+  /**
+   * Waits for a menu item's action to run. Items that let the menu close first defer their action
+   * to the next tick, and waitForServer is not a substitute: it sees nothing in flight in that gap.
+   */
+  export async function waitForMenuAction(optTimeout: number = 2000) {
+    await waitForPendingOps("testNumPendingMenuActions", "a menu action to run", optTimeout);
+  }
+
   /** Gets the value from the select component */
   export async function getSelectValue(selector: string) {
     return await driver.find(`${selector} .test-select-row`).getText();
diff --git a/test/nbrowser/gristWebDriverUtils.ts b/test/nbrowser/gristWebDriverUtils.ts
index a3d5035de..a73e8405e 100644
--- a/test/nbrowser/gristWebDriverUtils.ts
+++ b/test/nbrowser/gristWebDriverUtils.ts
@@ -413,7 +413,7 @@ export class GristWebDriverUtils {
       return ([] as T[]).concat(...rowNums.map((_r, i) => columns.map(c => c[i])));
     }
 
-    const { col, rowNums, section, mapper = el => el.getText() }: IColSelect<any> = (
+    const { col, rowNums, section, mapper }: IColSelect<any> = (
       typeof colOrOptions === "object" ? colOrOptions :
         { col: colOrOptions, rowNums: _rowNums!, section: _section }
     );
@@ -427,11 +427,24 @@ export class GristWebDriverUtils {
     const colIndex = (typeof col === "number" ? col :
       await sectionElem.findContentWait(".column_name", this.exactMatch(col), 1000).index());
 
+    const selector = `.gridview_data_scroll .record:not(.column_names) .field:nth-child(${colIndex + 1})`;
+
+    if (!mapper) {
+      // Read the row numbers and the cells together, in the browser. Reading them one element at
+      // a time leaves gaps in which the section can be rebuilt, losing the elements just found.
+      const [visibleRowNums, texts] = await this.driver.executeScript<[number[], string[]]>(
+        (elem: HTMLElement, cellSelector: string) => [
+          Array.from(elem.querySelectorAll(".gridview_data_row_num"),
+            el => parseInt((el as HTMLElement).innerText, 10)),
+          // Trimmed: getText() trims, innerText does not, and cells are white-space: pre.
+          Array.from(elem.querySelectorAll(cellSelector), el => (el as HTMLElement).innerText.trim()),
+        ], sectionElem, selector);
+      return rowNums.map(n => texts[visibleRowNums.indexOf(n)]) as T[];
+    }
+
     const visibleRowNums: number[] = await sectionElem.findAll(".gridview_data_row_num",
       async el => parseInt(await el.getText(), 10));
-
-    const selector = `.gridview_data_scroll .record:not(.column_names) .field:nth-child(${colIndex + 1})`;
-    const fields = mapper ? await sectionElem.findAll(selector, mapper) : await sectionElem.findAll(selector);
+    const fields = await sectionElem.findAll(selector, mapper);
     return rowNums.map(n => fields[visibleRowNums.indexOf(n)]);
   }
 
